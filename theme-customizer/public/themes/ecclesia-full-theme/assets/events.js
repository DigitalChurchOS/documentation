
lucide.createIcons();
const menuBtn=document.getElementById('menuBtn'),drawer=document.getElementById('drawer');
if(menuBtn&&drawer){menuBtn.onclick=()=>{drawer.classList.toggle('open');menuBtn.innerHTML=drawer.classList.contains('open')?'<i data-lucide="x"></i>':'<i data-lucide="menu"></i>';lucide.createIcons();};}
const toast=document.getElementById('toast');function showToast(m){toast.textContent=m;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1800)}
const target=new Date();target.setDate(target.getDate()+18);target.setHours(9,0,0,0);function tick(){const diff=Math.max(0,target-new Date()),d=Math.floor(diff/86400000),h=Math.floor(diff/3600000)%24,m=Math.floor(diff/60000)%60,s=Math.floor(diff/1000)%60;[['days',d],['hours',h],['minutes',m],['seconds',s]].forEach(([id,val])=>{const el=document.getElementById(id);if(el)el.textContent=String(val).padStart(2,'0')})}tick();setInterval(tick,1000);
const grid=document.getElementById('eventGrid');if(grid){const search=document.getElementById('searchInput'),cat=document.getElementById('categoryFilter'),type=document.getElementById('typeFilter'),price=document.getElementById('priceFilter'),mode=document.getElementById('modeFilter'),sort=document.getElementById('sortFilter'),count=document.getElementById('resultCount'),empty=document.getElementById('emptyState');function metas(){return Array.from(document.querySelectorAll('[name="meta"]:checked')).map(i=>i.value)}function apply(){const q=(search.value||'').toLowerCase(),c=cat.value,t=type.value,p=price.value,mo=mode.value,m=metas();let visible=0;Array.from(grid.children).forEach(card=>{const ok=(!q||card.dataset.title.toLowerCase().includes(q)||card.dataset.tags.toLowerCase().includes(q))&&(!c||card.dataset.category===c)&&(!t||card.dataset.type===t)&&(!p||card.dataset.price===p)&&(!mo||card.dataset.mode===mo)&&(!m.length||m.every(x=>card.dataset.tags.includes(x)));card.style.display=ok?'':'none';if(ok)visible++});Array.from(grid.children).sort((a,b)=>sort.value==='oldest'?Number(b.dataset.order)-Number(a.dataset.order):Number(a.dataset.order)-Number(b.dataset.order)).forEach(c=>grid.appendChild(c));count.textContent=visible+' events';empty.classList.toggle('show',visible===0)}[search,cat,type,price,mode,sort,...document.querySelectorAll('[name="meta"]')].forEach(el=>el.addEventListener('input',apply));document.querySelectorAll('[name="view"]').forEach(r=>r.addEventListener('change',()=>grid.classList.toggle('list',r.value==='list')));document.querySelectorAll('.cat').forEach(btn=>btn.onclick=()=>{document.querySelectorAll('.cat').forEach(b=>b.classList.remove('active'));btn.classList.add('active');cat.value=btn.dataset.category;apply()});document.getElementById('clearFilters').onclick=()=>{search.value='';cat.value='';type.value='';price.value='';mode.value='';sort.value='newest';document.querySelectorAll('[name="meta"]').forEach(i=>i.checked=false);document.querySelector('[name="view"][value="grid"]').checked=true;grid.classList.remove('list');document.querySelectorAll('.cat').forEach(b=>b.classList.remove('active'));document.querySelector('.cat').classList.add('active');apply()};apply();}
document.querySelectorAll('.open-event').forEach(btn=>btn.addEventListener('click',()=>(window.navigateToPage?window.navigateToPage('event-single.html'):location.href='event-single.html')));document.querySelectorAll('.open-register').forEach(btn=>btn.addEventListener('click',()=>(window.navigateToPage?window.navigateToPage('event-register.html'):location.href='event-register.html')));document.querySelectorAll('[data-rsvp]').forEach(btn=>btn.addEventListener('click',()=>showToast('RSVP saved')));document.querySelectorAll('[data-register]').forEach(btn=>btn.addEventListener('click',()=>showToast('Registration started')));document.querySelectorAll('[data-ticket]').forEach(btn=>btn.addEventListener('click',()=>showToast('Ticket downloaded')));document.querySelectorAll('[data-calendar]').forEach(btn=>btn.addEventListener('click',()=>showToast('Added to calendar')));document.querySelectorAll('[data-reminder]').forEach(btn=>btn.addEventListener('click',()=>showToast('Reminder enabled')));document.querySelectorAll('[data-share]').forEach(btn=>btn.addEventListener('click',()=>{navigator.clipboard?.writeText(location.href);showToast('Event link copied')}));document.querySelectorAll('.ticket-type').forEach(t=>t.addEventListener('click',()=>{document.querySelectorAll('.ticket-type').forEach(x=>x.classList.remove('active'));t.classList.add('active');const input=t.querySelector('input');if(input)input.checked=true;const total=document.getElementById('totalPrice');if(total)total.textContent=t.dataset.price||'Free'}));
