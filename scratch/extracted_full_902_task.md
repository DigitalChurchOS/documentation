# Page Builder in Customizer — Fix Tasks

- `[ ]` **1. Replace demo page content in local-preview.js** — Convert legacy schemas to Page Builder flexbox element trees
- `[ ]` **2. Robust content parsing in page-builder/app.js** — Handle both string and array content formats
- `[ ]` **3. Add loading indicator in customizer iframe area** — Show spinner while page builder loads
- `[ ]` **4. Add fallback renderer for unknown element types in Canvas.js** — Show placeholder for legacy blocks
- `[ ]` **5. Verify** — Start servers and test the full flow
