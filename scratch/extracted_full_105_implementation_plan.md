"# Implementation Plan - Unified Website Editing Studio\n\nOur goal is to update, reconstruct, improve, and properly integrate the existing Page Builder into the existing Theme Customizer to form a single, cohesive **Website Editing Studio**.\n\nThe existing Theme Customizer remains the parent experience, and all Page Builder features will adapt to it naturally.\n\n---\n\n## Proposed Database Schema Changes\n\nTo support the draft, preview, and publish flows for both theme customizations and pages (preventing editor changes from immediately updating the live site), we will extend the database schema in [schema.prisma](file:///C:/Users/Administrator/Documents/ChurchOS/prisma/schema.prisma):\n\n### 1. `Theme` Model Extension\n- Add `draftSettings String? @map(\"draft_settings\")` to store theme customization drafts.\n- The existing `settings` column will continue to store the published/live settings.\n\n### 2. `Page` Model Extension\n- Add `draftContent String? @map(\"draft_content\")` to store page layout/block drafts.\n- The existing `content` column will continue to store the published/live page layout.\n\n---\n\n## Proposed Backend API Changes\n\n### 1. Theme Engine Customization APIs ([themeEngine.ts](file:///C:/Users/Administrator/Documents/ChurchOS/src/routes/themeEngine.ts))\nWe will implement/update the following endpoints:\n- `PATCH /api/theme-engine/themes/:themeId/customization/draft`\n  Saves the theme customization settings into the `draftSettings` field of the tenant's Theme record.\n- `POST /api/theme-engine/themes/:themeId/customization/publish`\n  Promotes the `draftSettings` to the live `settings` field, records who published it, and creates an audit log.\n- `POST /api/theme-engine/themes/:themeId/customization/discard`\n  Clears the `draftSettings` and reverts to the live `settings`.\n- `POST /api/theme-engine/themes/:themeId/customization/reset`\n  Resets both draft and live settings to the theme's base settings.\n\n### 2. Page Builder CMS APIs ([cms.ts](file:///C:/Users/Administrator/Documents
<truncated 5050 bytes>