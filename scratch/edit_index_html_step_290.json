{
  "name": "replace_file_content",
  "args": {
    "AllowMultiple": "false",
    "Description": "\"Implement unified customizer-builder integration Javascript routing, tab controllers, dynamic page list loader, postMessage bindings, and draft/publish orchestrator\"",
    "EndLine": "16436",
    "Instruction": "\"Implement tab selectors, postMessage messaging bridge, page listing, page creator, dynamic property rendering, and studio save/publish handlers\"",
    "ReplacementContent": "\"      // Customize Theme button\\n      const customizeBtn = document.getElementById('customizeBtn');\\n      if (customizeBtn) {\\n        customizeBtn.onclick = () => {\\n          const activeTheme = themeEngineState.themes.find(t => t.id === themeEngineState.activeThemeId);\\n          if (activeTheme) {\\n            document.getElementById('customizerThemeName').textContent = activeTheme.name;\\n            let settings = {};\\n            try {\\n              settings = typeof activeTheme.settings === 'string' ? JSON.parse(activeTheme.settings) : (activeTheme.settings || {});\\n            } catch(e) {}\\n            customizerState.colorMode = settings.colorMode || 'light';\\n            customizerState.accentPreset = settings.accentPreset || 'orange';\\n            customizerState.colorPalette = settings.colorPalette || 'warm';\\n            customizerState.edgeStyle = settings.edgeStyle || 'soft';\\n            customizerState.cardStyle = settings.cardStyle || 'shadow';\\n            customizerState.spacingScale = settings.spacingScale || 'comfortable';\\n            customizerState.backgroundStyle = settings.backgroundStyle || 'softGradient';\\n            customizerState.iconStyle = settings.iconStyle || 'lucideLine';\\n            customizerState.buttonStyle = settings.buttonStyle || 'solid';\\n          }\\n          syncCustomizerUI();\\n          applyCustomizerTheme();\\n          document.getElementById('customizer').classList.add('active');\\n          document.body.classList.add('customizer-open');\\n          setStageDevice('desktop');\\n          \\n          // Show default Theme Design tab and seed/retrieve website default pages\\n          showTab('ThemeDesign');\\n          loadCustomizerPages();\\n        };\\n      }\\n\\n      // Close customizer button\\n      const closeCustomizer = document.getElementById('closeCustomizer');\\n      if (closeCustomizer) {\\n        closeCustomizer.onclick = () => {\\n          document.getElementById('customizer').classList.remove('active');\\n          document.body.classList.remove\n<truncated 22792 bytes>",
    "StartLine": "16373",
    "TargetContent": "\"      // Customize Theme button\\n      const customizeBtn = document.getElementById('customizeBtn');\\n      if (customizeBtn) {\\n        customizeBtn.onclick = () => {\\n          const activeTheme = themeEngineState.themes.find(t => t.id === themeEngineState.activeThemeId);\\n          if (activeTheme) {\\n            document.getElementById('customizerThemeName').textContent = activeTheme.name;\\n            let settings = {};\\n            try {\\n              settings = typeof activeTheme.settings === 'string' ? JSON.parse(activeTheme.settings) : (activeTheme.settings || {});\\n            } catch(e) {}\\n            customizerState.colorMode = settings.colorMode || 'light';\\n            customizerState.accentPreset = settings.accentPreset || 'orange';\\n            customizerState.colorPalette = settings.colorPalette || 'warm';\\n            customizerState.edgeStyle = settings.edgeStyle || 'soft';\\n            customizerState.cardStyle = settings.cardStyle || 'shadow';\\n            customizerState.spacingScale = settings.spacingScale || 'comfortable';\\n            customizerState.backgroundStyle = settings.backgroundStyle || 'softGradient';\\n            customizerState.iconStyle = settings.iconStyle || 'lucideLine';\\n            customizerState.buttonStyle = settings.buttonStyle || 'solid';\\n          }\\n          syncCustomizerUI();\\n          applyCustomizerTheme();\\n          document.getElementById('customizer').classList.add('active');\\n          document.body.classList.add('customizer-open');\\n          setStageDevice('desktop');\\n        };\\n      }\\n\\n      // Close customizer button\\n      const closeCustomizer = document.getElementById('closeCustomizer');\\n      if (closeCustomizer) {\\n        closeCustomizer.onclick = () => {\\n          document.getElementById('customizer').classList.remove('active');\\n          document.body.classList.remove('customizer-open');\\n        };\\n      }\\n\\n      // Hide / Reveal Topbar\\n      const hideTopbar = document.getElementById('hideTopbar');\\n      if (hideTopbar) {\\\n<truncated 839 bytes>",
    "TargetFile": "\"C:\\\\Users\\\\Administrator\\\\Documents\\\\ChurchOS\\\\apps\\\\tenant-dashboard\\\\public\\\\index.html\"",
    "toolAction": "\"Integrating Page Builder controls and script logic inside Customizer Dashboard\"",
    "toolSummary": "\"index.html script edit\""
  }
}