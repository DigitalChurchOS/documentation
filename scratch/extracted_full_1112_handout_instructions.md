"# Developer Hand-off: Fixing Page Builder Integration in Theme Customizer\n\nThis document outlines the requirements, root cause analysis, implementation steps, and verification procedures for integrating the Page Builder into the Theme Customizer of the Digital Church OS SaaS platform.\n\n---\n\n## 1. Overview & Objective\nThe goal is to seamlessly wire the visual **Page Builder** into the **Theme Customizer**'s \"Pages\" tab within the local preview development environment. \n\nWhen a developer opens the Theme Customizer and switches to the **Pages** tab:\n1. The page list must load.\n2. Selecting a page must load the Page Builder inside an iframe, rendering the page layout canvas.\n3. Clicking on elements inside the canvas must update the parent dashboard's inspector sidebar for real-time adjustments.\n\n---\n\n## 2. System Architecture & Context\nThe implementation spans three distinct layers:\n1. **Mock Preview Server (`scripts/local-preview.js`)**: Hosts local mock database states and serves the API routes (`/api/cms/pages`, `/api/cms/pages/:id`, etc.).\n2. **Parent Customizer Dashboard (`apps/tenant-dashboard/public/index.html`)**: The main admin panel where the user selects pages, configures device toggles, and edits element properties in the sidebar.\n3. **Embedded Page Builder Canvas (`page-builder/`)**: A React/TypeScript application running in `/page-builder/?mode=embed`, communicating with the parent admin panel via HTML5 `postMessage` bridges.\n\n---\n\n## 3. Problem Diagnosis & Root Causes\nBefore this fix, the Page Builder iframe rendered as a blank white box because of three core mismatches:\n1. **Schema Mismatch (Legacy CMS vs. Page Builder)**:\n   - The mock database seed pages inside `local-preview.js` used a flat legacy template schema:\n     ```javascript\n     content: [{ type: 'hero', title: '...', subtitle: '...' }]\n     ```\n   - The React-based Page Builder's `Canvas.js` expected a hierarchical flexbox node layout:\n     ```javascript\n     content: [{ id: 'flex_hero', type: 'flexbox'
<truncated 6714 bytes>